import { OfferPercentagePipe } from './offer-percentage.pipe';

describe('OfferPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
